# Handle peer dependency issues
legacy-peer-deps=true

# Disable strict SSL for certain packages if needed
# strict-ssl=false

# Use faster package manager settings
prefer-offline=true
audit=false

# Fix potential MediaPipe issues
resolve-conflicts=newest 